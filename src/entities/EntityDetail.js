import { Helmet } from "react-helmet-async";

function EntityDetail() {
    return (
        <>
            <Helmet>
                <title>Entities | Campaign Manager</title>
                <meta name="description" content="Manage your entities (eg. characters, items, etc.)" />
            </Helmet>

            <div>Entity Details Page Coming Soon</div>
        </>
    );
}

export default EntityDetail;